import styled from "styled-components";

function Progress() {
  return <ProgressContainer>進度：</ProgressContainer>;
}

export default Progress;

const ProgressContainer = styled.div`
  font-size: 15px;
  font-weight: 400;
`;
